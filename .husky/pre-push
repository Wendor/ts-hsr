npm run build
git checkout --orphan build
rm -rf .husky node_modules
git reset .
git add -f dist
git checkout master -- index.html
git diff --quiet HEAD || git commit -m 'update build'
git push --force origin build
git clean  -d  -f .
git checkout master
git branch -D build
